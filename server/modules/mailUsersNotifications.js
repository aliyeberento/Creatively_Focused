const pool = require('./pool');
const sendEmail = require('./mailer');

//email all users their daily deadline reminders
module.exports = () => {
    console.log('firing');
    //moved query to a router ??

}